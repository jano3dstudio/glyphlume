# Repository scope

Visibility: PRIVATE. No open-source license selected.

The canonical checkout is `JS_GitHub/glyphlume` in the local project storage. Since 2026-09-21 the tested app is delivered as `GLYPHLUME.exe` in this checkout, with a clean portable ZIP under `dist`. The former installation at `JS_GPT_Projects/local-icon-generator` remains a fallback; use the new start path. See `00_START_HERE.md`. This repository contains reviewed source and product assets. Do not copy the entire installed working folder into this repository: it contains personal data and historical backups. Refresh only the reviewed source list and product assets. Personal data and Icons were copied locally with SHA-256 verification; they remain ignored by Git and excluded from the ZIP. Existing shortcuts may still reference the old installation, so it is retained.

Current source includes GLYPHLUME branding, left-aligned header logo, LinkedIn creator link, icon libraries and reference images. Existing Windows app icon intentionally remains TGC_SEP26_2.ico.
